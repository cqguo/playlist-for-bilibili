# B站歌单模式

点击插件图标后，可以选择进入：

- 将当前 B 站视频添加到默认歌单
- 歌单管理界面
- 歌单选择界面

内置一个包含 100 首华语歌曲的默认歌单，新增视频会保存在浏览器本地。
从歌单打开视频时，在页面
右侧显示播放列表。播放列表使用 Shadow DOM 隔离，并挂载在
`body` 下，不修改 B 站 Header 或其他原生组件。
每个歌单的播放模式独立保存在浏览器本地，修改后会在后续页面和浏览器会话中继续生效。
