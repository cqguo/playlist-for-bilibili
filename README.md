# 自定义播放列表

点击插件图标后，可以选择进入：

- 将当前 B 站视频添加到自定义播放列表
- 播放列表管理界面
- 播放列表选择界面

内置一个包含 100 个华语音乐视频的默认播放列表，新增视频会保存在浏览器本地。
从播放列表打开视频时，在页面
右侧显示播放列表。播放列表使用 Shadow DOM 隔离，并挂载在
`body` 下，不修改 B 站 Header 或其他原生组件。
每个播放列表的播放模式独立保存在浏览器本地，修改后会在后续页面和浏览器会话中继续生效。
